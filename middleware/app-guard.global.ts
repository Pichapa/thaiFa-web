export default defineNuxtRouteMiddleware((to, from) => {
  // if (
  //   !to.path.includes("authentication/login") &&
  //   !localStorage.getItem("acessToken")
  // ) {
  //   return navigateTo("/authentication/login");
  // }
  // return;
});
